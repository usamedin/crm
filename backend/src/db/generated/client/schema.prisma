generator client {
  provider = "prisma-client-js"
  output   = "../src/db/generated/client"
}

datasource crm {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Product {
  id   Int @id
  name String
}
